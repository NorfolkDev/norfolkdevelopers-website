const siteConfig = {
  siteName: "Next.js blog",
  description: "Make blogs with next.",
  postsDirectory: "words",
  features: {
    authorPages: true,
    tagPages: true
  }
};

export default siteConfig;
